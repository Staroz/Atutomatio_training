#Automation_training 

In the file cypress.config.js, fill in the fields of the object "credentials" 
like:

env: { 
  credentials: { 
    email: "example@email.com", 
    pw: "MyPassword",
  } 
}



